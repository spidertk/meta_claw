# 干净状态检查清单

> 最后核对：2026-05-15  
> 结论：当前仓库**还不是干净状态**，但真实边界已经记录清楚。

- [ ] 标准启动路径仍然可用  
  证据：`./init.sh` 已改为 Maven 工作流，但 2026-05-15 当前环境缺少 `mvn`，所以还不能完整走通
- [ ] 标准验证路径仍然可运行  
  证据：`mvn test` 于 2026-05-15 失败；当前环境返回 `mvn: command not found`
- [x] 当前进度已经记录到进度日志  
  证据：`claude-progress.md` 已于 2026-05-15 更新为当前实现与 blocker
- [x] 功能状态真实反映了 passing 和未验证的边界  
  证据：`feature_list.json` 已区分“代码已实现”“历史上有测试报告”“本轮无法重新验证”
- [ ] 没有任何半成品步骤处于未记录状态  
  证据：`Expert/专家` 残留、`ChatCommand` 未恢复历史、`package-lock.json` 未跟踪均已记录，但尚未处理
- [ ] 下一轮会话无需人工修复即可继续  
  证据：下一轮在开始功能前，仍需先补齐 Maven 可执行入口

## 进入下一轮前必须先确认

1. `git status --short` 中的 `package-lock.json` 已被有意识地保留或清理
2. `./init.sh` 已改为仓库真实启动/验证路径，并能在当前环境真实跑通
3. 标准验证命令已经实际跑过，而不是只引用旧的 `target/surefire-reports`
