# Python Workers

This directory contains Sprint 1 worker skeletons.

Current scope:

- contract references
- shared example fixtures for contract validation
- a stub entry point that returns sample result payloads

This round does not invoke real ingest, graphify, or wiki generation.

`knowledge/examples` is not runtime knowledge storage. In Sprint 1 it only exists as
shared fixtures for fake end-to-end validation across Java and Python.
