# Examples

Python workers can use the shared examples in `knowledge/examples` for contract validation.

These files are fixture inputs and outputs for Sprint 1. They are not the long-term
runtime storage location for worker jobs or artifacts.

The registry fixture lives in `knowledge/examples/knowledge-registry.example.json`.
