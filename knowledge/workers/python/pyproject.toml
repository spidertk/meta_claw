[project]
name = "knowledge-python-workers"
version = "0.1.0"
description = "Sprint 1 worker skeletons for the private knowledge base"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
